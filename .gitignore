# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# Jupyter Notebooks
.ipynb_checkpoints

# Data (Exclude large files, keep structure)
data/raw/*
data/processed/*
data/augmented/*
!data/raw/.gitkeep
!data/processed/.gitkeep
!data/augmented/.gitkeep

# Splits: ignore the copied images, keep the manifests and the report so the
# partition stays reproducible (split_dataset.py rebuilds the images from them)
data/splits/*/
!data/splits/*_manifest.csv
!data/splits/split_report.json

# Models (Exclude heavy weights)
models/checkpoints/*
!models/checkpoints/.gitkeep
*.h5
*.keras
*.tflite

# Arduino / PlatformIO
.pio/
.pioenvs/
.piolibdeps/
*.elf
*.hex
*.bin
*.map

# OS
.DS_Store
Thumbs.db

# Trabajo aislado fuera del arbol principal: puerto a Arduino Nicla Vision
# (sketches nuevos + copias de los scripts del host + parche contra 3db7bde).
# Son ~4 MB, dominados por los dos model.h. Quitar esta regla cuando se decida
# integrarlo; ver nicla_vision_port/_LEEME.md
nicla_vision_port/
